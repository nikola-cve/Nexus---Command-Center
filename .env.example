# Nexus Command Center environment variables
# Copy to .env.local for local dev. In production set these in the Vercel project.
# Never commit real secrets.

# Supabase (Step 2). Server-only access, so the service_role key stays on the server.
SUPABASE_URL=https://rjgzksjulqqbetzjwcpl.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Claude engine (Step 3). Pay-as-you-go API key, not a Max/Pro subscription.
ANTHROPIC_API_KEY=
